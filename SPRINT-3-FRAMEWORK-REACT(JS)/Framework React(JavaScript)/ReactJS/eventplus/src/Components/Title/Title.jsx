import React from 'react';

import './Title.css';


const Title = () => {
    return (
            <h1 className={`Title ${className}`} style={{ color : color}}>
            
            <hr 
            className='Title'
            />
            </h1>
    );
};

export default Titulo;